<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".presentation.app.HealthHubApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HealthHub"
        tools:targetApi="31">

        <activity
            android:name="com.example.healthhub.presentation.app.AuthenticationActivity"
            android:exported="true"
            android:theme="@style/Theme.HealthHub">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.example.healthhub.presentation.app.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.HealthHub" />

        <provider
            android:name="com.example.healthhub.domain.provider.CameraFileProvider"
            android:authorities="com.example.healthhub.camera.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/filepaths" />
        </provider>
    </application>

</manifest>